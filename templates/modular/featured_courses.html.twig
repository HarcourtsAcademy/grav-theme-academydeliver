<h2>{{ page.header.title }}</h2>

  {% block content %}

    {{ page.content }}

    {% for post in taxonomy.findTaxonomy({'category':'course','tag':'featured'}) %}
        <h3><a href="{{ post.url }}">{{ post.title }}</a></h3>
        <p>{{ post.summary }} <a class="button" href="{{ post.url }}">Read more</a></p>
    {% endfor %}

  {% endblock %}