{% embed 'partials/base.html.twig' %}

  {% block content %}

    {{ page.content }}

    <h2>Regions</h2>
    <ul>
    {% for post in taxonomy.findTaxonomy({'category':'qualification-region'}) %}
        <li><a href="{{ base_url }}/get-qualified/{{ post.slug }}">{{ post.title }}</a>
        </li>
    {% endfor %}
    <ul>

  {% endblock %}

{% endembed %}